# dotman Design Docs

This directory is split by concern:

- `config.md`: user-level dotman manager configuration
- `repository.md`: repository structure and configuration schema
- `cli.md`: selector resolution, command behavior, and stateful workflow
- `snapshot.md`: push snapshot lifecycle, retention, and rollback semantics

The example repo under `../examples/repo/` is the concrete reference for both.
