# Examples

This directory contains concrete example layouts for the new `dotman` model.

- `repo/`: a minimal repo showing packages, profiles, and local overrides

Design rules and current semantics live in:

- `../docs/config.md`
- `../docs/repository.md`
- `../docs/cli.md`
