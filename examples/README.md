# Examples

This directory contains concrete example layouts for the new `dotman` model.

- `repo/`: a minimal repo showing packages, profiles, and an example local override file shape; real machine-local overrides live under XDG config

Design rules and current semantics live in:

- `../docs/config.md`
- `../docs/repository.md`
- `../docs/templates.md`
- `../docs/cli.md`
